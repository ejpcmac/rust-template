{{license-header}}
//! {{description}}

use eyre::Result;

use {{crate_name}}::{{crate_name|upper_camel_case}};

fn main() -> Result<()> {
    color_eyre::install()?;
    {{crate_name|upper_camel_case}}::run()
}
