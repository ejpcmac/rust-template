{{license-header}}
//! {{description}}

{{module-attributes}}
use defmt_rtt as _;
use panic_probe as _;

use cortex_m_rt::entry;
use stm32l4xx_hal as _;

#[entry]
fn main() -> ! {
    defmt::println!("Hello, world!");

    loop {
        cortex_m::asm::wfi();
    }
}
